# Fixes Applied

## Top 5 Restaurants on Homepage

- Created `js/home.js` to dynamically load the top 5 restaurants on the homepage.
- Modified `index.html` to remove the hardcoded list of restaurants and replaced it with a dynamic list loaded from Supabase.
- Included `js/restaurants.js` and `js/home.js` in `index.html` to support the dynamic loading of restaurants.
- Removed the unused `js/main.js` script from `index.html`.